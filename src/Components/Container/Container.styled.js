import styled from "@emotion/styled";

export const Wrapper = styled.div`
  width: 1240px;
  height: 1754px;
  background: var(--white-color);
  display: flex;
  margin: 0 auto;
  border-right: 1px solid var(--light-color);
  /* -webkit-box-shadow: 0px 5px 35px 10px var(--light-color);
  -moz-box-shadow: 0px 5px 35px 10px var(--light-color);
  box-shadow: 0px 5px 35px 10px var(--light-color); */
`;
